export const rotpsi = (psi) => {
  return [[0, 0, 0], [0, 0, 0], [0, 0, 0]];
};

export const angle_to_two_pi = (angle) => {
  return 0;
};

export class MinimalVessel {
  constructor(data) {
    Object.assign(this, data);
  }
}
